import React, { Fragment, useEffect } from "react";
import Layout from "../layout";


const Success = (props) => {
   
  return (
    <Fragment>
      <Layout   />
    </Fragment>
  );
};

export default Success;
